{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "overlay-capability",
  "description": "Flow overlay window capabilities",
  "windows": ["overlay"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:default"
  ]
}
