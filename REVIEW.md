Cuts:
In `@src-tauri/gen/schemas/`:
- Entire directory: `delete` Stop tracking the 305 KB of Tauri-generated schemas. They are regenerated by Tauri tooling and are not build inputs; ignore the directory and remove the local `$schema` reference from `src-tauri/capabilities/default.json`. (-4,666 lines)

In `@package-lock.json`:
- Around lines 802-896: `dependency` Regenerate the lockfile after removing Radix Progress. Its five package entries exist only to render one progress container and fill element. (-96 lines)

In `@plans/dictation-reliability-fixes.md`:
- Entire file: `delete` Remove the completed implementation plan. Every listed change and verification test is already represented in the current source and tests, and nothing references this artifact. (-28 lines)

In `@src-tauri/src/database.rs`:
- Around lines 298-321: `delete` Remove `insert_history`, which duplicates the transaction in `save_pending_to_history` and has no production caller. Update the weekly-retention test to create and commit a pending dictation through the production path. (-23 lines)

In `@src/pages/Snippets.tsx`:
- Around lines 34-42 and 139-151: `collapse` Remove both window-level Escape effects and pass the existing `close`/`closeAddDialog` callbacks to `useDialogFocus`, which already handles Escape for dialogs. (-22 lines)

In `@src/pages/Dictionary.tsx`:
- Around lines 27-39: `collapse` Remove the duplicate window-level Escape listener and let `useDialogFocus` invoke `closeAddDialog`. (-13 lines)

In `@src/components/EditableRow.tsx`:
- Around lines 31-39: `collapse` Remove the duplicate window-level Escape listener and pass `close` to `useDialogFocus`. (-10 lines)

In `@src/Overlay.tsx`:
- Lines 3 and 167-176: `dependency` Replace the sole Radix Progress use with the same two elements plus `role="progressbar"`, `aria-valuenow`, `aria-valuemin`, and `aria-valuemax`. The existing CSS and progress state already provide all behavior. (-1 line)

In `@package.json`:
- Line 17: `dependency` Remove `@radix-ui/react-progress` after using the native ARIA progress structure in `Overlay`. (-1 line)

In `@src-tauri/Cargo.lock`:
- Lines 1131 and 1629-1633: `dependency` Regenerate the lockfile after removing the unused `hound` crate. (-6 lines)

In `@src-tauri/Cargo.toml`:
- Line 26: `dependency` Remove `hound`; WAV encoding is implemented directly in `audio.rs`, and the crate has no caller. (-1 line)
- Around lines 39-50: `shrink` Remove the unused Windows feature flags for Globalization, GDI, ToolHelp, Threading, and Shell. No source imports APIs from those namespaces. (-5 lines)

In `@src/styles.css`:
- Lines 185-188 and 245: `delete` Remove the unused `section-heading`, `count-pill`, and `helper-copy` rules. No component emits those classes. (-4 lines)

In `@src/components/Logo.tsx`:
- Around lines 1-13: `yagni` Remove the `compact` prop, conditional class, and conditional wordmark branch. The only caller always renders the full logo. (-4 lines)

In `@src-tauri/src/workflow.rs`:
- Lines 165-167: `inline` Remove `toggle_from_tray`, which is identical to `toggle`; have the tray handler call `toggle` directly. (-3 lines)

In `@src-tauri/src/audio.rs`:
- Lines 125-127: `delete` Remove `AudioRecorder::is_recording`; the workflow state machine replaced this check and there are no callers. (-3 lines)

In `@src/api.ts`:
- Lines 43-45: `delete` Remove the unused `startRecording`, `stopRecording`, and `cancelRecording` client wrappers. Recording is driven by the native keyboard/tray handlers and no frontend code calls these methods. (-3 lines)

In `@flow-logo.png` and `@src-tauri/icons/`:
- Entire unused asset set: `delete` Remove `flow-logo.png` and the 49 Tauri icon files not named by the Windows-only bundle configuration. Keep only `32x32.png`, `128x128.png`, `128x128@2x.png`, and `icon.ico`. (-50 files)
